diff --git a/src/compositor/noctalia_compositor.c b/src/compositor/noctalia_compositor.c
index 96d4724..7700820 100644
--- a/src/compositor/noctalia_compositor.c
+++ b/src/compositor/noctalia_compositor.c
@@ -12,6 +12,7 @@
 #include <stdlib.h>
 #include <string.h>
 #include <sys/wait.h>
+#include <time.h>
 #include <unistd.h>
 #include <wayland-server-core.h>
 #include <wayland-server-protocol.h>
@@ -123,6 +124,10 @@ struct greeter_server {
   float manual_scale;
   int manual_mode_width;
   int manual_mode_height;
+  int idle_timeout_sec;
+  struct timespec last_activity;
+  struct wl_event_source* idle_timer;
+  bool screens_blanked;
   char cursor_theme[128];
   int cursor_size;
   char cursor_path[512];
@@ -261,11 +266,16 @@ static void parse_output_layout_value(struct greeter_server* server, char* value
   }
 }
 
+static void note_activity(struct greeter_server* server);
+static void wake_outputs(struct greeter_server* server);
+static void blank_active_outputs(struct greeter_server* server);
+
 static void read_greeter_config(struct greeter_server* server) {
   server->preferred_output[0] = '\0';
   server->manual_scale = 0.0f;
   server->manual_mode_width = 0;
   server->manual_mode_height = 0;
+  server->idle_timeout_sec = 0;
   server->cursor_theme[0] = '\0';
   server->cursor_size = 0;
   server->cursor_path[0] = '\0';
@@ -290,6 +300,9 @@ static void read_greeter_config(struct greeter_server* server) {
   if (config.manual_mode_height > 0) {
     server->manual_mode_height = config.manual_mode_height;
   }
+  if (config.idle_timeout_sec > 0) {
+    server->idle_timeout_sec = config.idle_timeout_sec;
+  }
   if (config.cursor_theme[0] != '\0') {
     snprintf(server->cursor_theme, sizeof(server->cursor_theme), "%s", config.cursor_theme);
   }
@@ -604,6 +617,65 @@ static void handle_output_request_state(struct wl_listener* listener, void* data
   wlr_output_commit_state(output->wlr_output, event->state);
 }
 
+static void note_activity(struct greeter_server* server) {
+  clock_gettime(CLOCK_MONOTONIC, &server->last_activity);
+  if (server->screens_blanked) {
+    wake_outputs(server);
+  }
+}
+
+static void blank_active_outputs(struct greeter_server* server) {
+  struct greeter_output* output;
+  wl_list_for_each(output, &server->outputs, link) {
+    if (!output->active || !output->wlr_output->enabled) {
+      continue;
+    }
+    struct wlr_output_state state;
+    wlr_output_state_init(&state);
+    wlr_output_state_set_enabled(&state, false);
+    wlr_output_commit_state(output->wlr_output, &state);
+    wlr_output_state_finish(&state);
+  }
+  server->screens_blanked = true;
+  wlr_log(WLR_INFO, "idle: blanked active outputs");
+}
+
+static void wake_outputs(struct greeter_server* server) {
+  struct greeter_output* output;
+  wl_list_for_each(output, &server->outputs, link) {
+    if (!output->active || output->wlr_output->enabled) {
+      continue;
+    }
+    struct wlr_output_state state;
+    wlr_output_state_init(&state);
+    wlr_output_state_set_enabled(&state, true);
+    wlr_output_commit_state(output->wlr_output, &state);
+    wlr_output_state_finish(&state);
+    if (output->scene_output != NULL) {
+      wlr_output_schedule_frame(output->wlr_output);
+    }
+  }
+  server->screens_blanked = false;
+  schedule_output_frames(server);
+  wlr_log(WLR_INFO, "idle: woke outputs");
+}
+
+static int handle_idle_timer(void* data) {
+  struct greeter_server* server = data;
+  if (server->idle_timeout_sec <= 0) {
+    return 0;
+  }
+  if (!server->screens_blanked) {
+    struct timespec now;
+    clock_gettime(CLOCK_MONOTONIC, &now);
+    const long elapsed = now.tv_sec - server->last_activity.tv_sec;
+    if (elapsed >= server->idle_timeout_sec) {
+      blank_active_outputs(server);
+    }
+  }
+  return 2000;
+}
+
 static void disable_output(struct greeter_output* output) {
   if (!output->active && !output->wlr_output->enabled) {
     return;
@@ -981,6 +1053,7 @@ static void pointer_focus(struct greeter_server* server, double lx, double ly) {
 static void handle_cursor_motion(struct wl_listener* listener, void* data) {
   struct greeter_server* server = wl_container_of(listener, server, cursor_motion);
   struct wlr_pointer_motion_event* event = data;
+  note_activity(server);
   wlr_cursor_move(server->cursor, &event->pointer->base, event->delta_x, event->delta_y);
   pointer_focus(server, server->cursor->x, server->cursor->y);
 }
@@ -988,6 +1061,7 @@ static void handle_cursor_motion(struct wl_listener* listener, void* data) {
 static void handle_cursor_motion_absolute(struct wl_listener* listener, void* data) {
   struct greeter_server* server = wl_container_of(listener, server, cursor_motion_absolute);
   struct wlr_pointer_motion_absolute_event* event = data;
+  note_activity(server);
   wlr_cursor_warp_absolute(server->cursor, &event->pointer->base, event->x, event->y);
   pointer_focus(server, server->cursor->x, server->cursor->y);
 }
@@ -995,6 +1069,7 @@ static void handle_cursor_motion_absolute(struct wl_listener* listener, void* da
 static void handle_cursor_button(struct wl_listener* listener, void* data) {
   struct greeter_server* server = wl_container_of(listener, server, cursor_button);
   struct wlr_pointer_button_event* event = data;
+  note_activity(server);
   pointer_focus(server, server->cursor->x, server->cursor->y);
   wlr_seat_pointer_notify_button(server->seat, event->time_msec, event->button, event->state);
 }
@@ -1047,6 +1122,7 @@ static void handle_keyboard_key(struct wl_listener* listener, void* data) {
     }
   }
 
+  note_activity(server);
   wlr_seat_keyboard_notify_key(server->seat, event->time_msec, event->keycode, event->state);
 }
 
@@ -1383,6 +1459,7 @@ int main(int argc, char** argv) {
   wl_list_init(&server.outputs);
   wl_list_init(&server.keyboards);
   read_greeter_config(&server);
+  clock_gettime(CLOCK_MONOTONIC, &server.last_activity);
 
   server.display = wl_display_create();
   if (server.display == NULL) {
@@ -1493,6 +1570,11 @@ int main(int argc, char** argv) {
 
   struct wl_event_loop* loop = wl_display_get_event_loop(server.display);
   server.sigchld_source = wl_event_loop_add_signal(loop, SIGCHLD, handle_sigchld, &server);
+  if (server.idle_timeout_sec > 0) {
+    server.idle_timer = wl_event_loop_add_timer(loop, handle_idle_timer, &server);
+    wl_event_source_timer_update(server.idle_timer, 2000);
+    wlr_log(WLR_INFO, "idle: screen blank after %d s of no input", server.idle_timeout_sec);
+  }
   server.child_argc = argc;
   server.child_argv_ptr = argv;
   schedule_launch(&server);
@@ -1506,6 +1588,9 @@ int main(int argc, char** argv) {
   if (server.sigchld_source != NULL) {
     wl_event_source_remove(server.sigchld_source);
   }
+  if (server.idle_timer != NULL) {
+    wl_event_source_remove(server.idle_timer);
+  }
   cleanup_server_listeners(&server);
   wl_display_destroy_clients(server.display);
   wl_display_destroy(server.display);
diff --git a/src/greeter/greeter_config_io.cpp b/src/greeter/greeter_config_io.cpp
index 593d559..8b3f11d 100644
--- a/src/greeter/greeter_config_io.cpp
+++ b/src/greeter/greeter_config_io.cpp
@@ -23,7 +23,8 @@ namespace {
         || key == "output"
         || key == "cursor"
         || key == "keyboard"
-        || key == "auth";
+        || key == "auth"
+        || key == "idle";
   }
 
   [[nodiscard]] bool isKnownSessionKey(std::string_view key) { return key == "default" || key == "last"; }
@@ -38,6 +39,8 @@ namespace {
     return key == "name" || key == "layout" || key == "scale" || key == "width" || key == "height";
   }
 
+  [[nodiscard]] bool isKnownIdleKey(std::string_view key) { return key == "timeout"; }
+
   [[nodiscard]] bool isKnownCursorKey(std::string_view key) { return key == "theme" || key == "size" || key == "path"; }
 
   [[nodiscard]] bool isKnownKeyboardKey(std::string_view key) {
@@ -84,6 +87,15 @@ namespace {
     return std::nullopt;
   }
 
+  [[nodiscard]] std::optional<int> idleTimeoutValue(const toml::node& node) {
+    if (const auto value = node.value<int64_t>()) {
+      if (*value >= 0 && *value <= 86400) {
+        return static_cast<int>(*value);
+      }
+    }
+    return std::nullopt;
+  }
+
   void warnUnknownTopLevelKey(const std::filesystem::path& path, std::string_view key) {
     kLog.warn("{}: unrecognized top-level key '{}' (ignored)", path.string(), key);
   }
@@ -164,6 +176,12 @@ namespace {
           } else if (entryView == "height") {
             config.outputModeHeight = modeDimensionValue(entryNode);
           }
+        } else if (keyView == "idle") {
+          if (!isKnownIdleKey(entryView)) {
+            warnUnknownSectionKey(path, keyView, entryView);
+            continue;
+          }
+          config.idleTimeoutSec = idleTimeoutValue(entryNode);
         } else if (keyView == "cursor") {
           if (!isKnownCursorKey(entryView)) {
             warnUnknownSectionKey(path, keyView, entryView);
@@ -384,7 +402,7 @@ namespace greeter::config {
     std::ostringstream out;
     out << "# noctalia-greeter greeter.toml\n";
     out << "# [session] default/last, [user] default, [appearance] scheme/password_style/hide_logo\n";
-    out << "# [output] name/layout/scale/width/height, [cursor] theme/size/path\n";
+    out << "# [output] name/layout/scale/width/height, [idle] timeout, [cursor] theme/size/path\n";
     out << "# [keyboard] layout/variant/options/numlock\n";
     out << "# [auth] allow_empty_password (bool, default false; enables fingerprint/smartcard PAM auth)\n";
     out << '\n';
@@ -437,6 +455,9 @@ extern "C" void greeter_compositor_config_load(const char* state_dir, struct gre
   if (config.outputModeHeight.has_value() && *config.outputModeHeight > 0) {
     out->manual_mode_height = *config.outputModeHeight;
   }
+  if (config.idleTimeoutSec.has_value() && *config.idleTimeoutSec >= 0) {
+    out->idle_timeout_sec = *config.idleTimeoutSec;
+  }
   if (config.cursorSize.has_value() && *config.cursorSize > 0) {
     out->cursor_size = *config.cursorSize;
   }
diff --git a/src/greeter/greeter_config_io.h b/src/greeter/greeter_config_io.h
index 09e8680..6441373 100644
--- a/src/greeter/greeter_config_io.h
+++ b/src/greeter/greeter_config_io.h
@@ -11,6 +11,7 @@ struct greeter_compositor_config {
   float manual_scale;
   int manual_mode_width;
   int manual_mode_height;
+  int idle_timeout_sec;
   char cursor_theme[128];
   int cursor_size;
   char cursor_path[512];
diff --git a/src/greeter/greeter_config_store.h b/src/greeter/greeter_config_store.h
index 0a55da5..f0cab73 100644
--- a/src/greeter/greeter_config_store.h
+++ b/src/greeter/greeter_config_store.h
@@ -22,6 +22,8 @@ namespace greeter::config {
     std::optional<int> outputModeWidth;
     std::optional<int> outputModeHeight;
 
+    std::optional<int> idleTimeoutSec;
+
     std::optional<std::string> cursorTheme;
     std::optional<int> cursorSize;
     std::optional<std::string> cursorPath;
