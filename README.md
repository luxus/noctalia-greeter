# noctalia-greeter

Local test wrapper for [noctalia-greeter](https://github.com/noctalia-dev/noctalia-greeter).

Currently pins upstream input to `luxus/noctalia-greeter-1` branch
`local/per-output-wallpaper` (per-output wallpapers + transform; not PR'd yet).

```sh
nix build .#noctalia-greeter
```
